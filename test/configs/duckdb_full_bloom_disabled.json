{
  "description": "Load Bloom for every DuckDB test database but keep Bloom disabled.",
  "statically_loaded_extensions": [
    "core_functions",
    "bloom"
  ],
  "summarize_failures": true,
  "skip_tests": [
    {
      "reason": "Bloom's extension self-test intentionally asserts that Bloom is enabled by default.",
      "paths": [
        "test/sql/bloom.test"
      ]
    },
    {
      "reason": "Prepared-cache persistence tests require enabled Bloom queries to create their sample files.",
      "paths": [
        "test/sql/sampling/prepared_seed_cache.test",
        "test/sql/sampling/prepared_target_cache.test"
      ]
    }
  ]
}
